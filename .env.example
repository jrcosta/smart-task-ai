# JWT Secret Key (mude em produção)
JWT_SECRET=sua-chave-secreta-jwt-muito-segura-aqui

# OpenAI API Key (OPCIONAL - pode ser configurado via interface web em /settings)
# Obtenha em https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Twilio WhatsApp (OPCIONAL - pode ser configurado via interface web em /settings)
# Obtenha em https://www.twilio.com/console
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# Database (opcional, já configurado no docker-compose)
POSTGRES_DB=smarttask
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
